# Dependencies
node_modules
.bun

# Build outputs & TS build info
dist
dist-ssr
*.local
*.tsbuildinfo

# usagi (task issue store: lock file and session worktrees are machine-local)
.usagi/issues/.lock
.usagi/sessions

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
bun.lockb
.DS_Store
